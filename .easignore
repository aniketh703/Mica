# Exclude local tooling dirs that contain symlinks/junctions
.claude/
.agents/

# Dev artifacts
node_modules/

# Local-machine-only Android build state (path leaks, and it's the bulk of the 795MB archive)
android/local.properties
android/build/
android/app/build/
android/.gradle/
