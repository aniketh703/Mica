// Fixed swatch of user-selectable event colors. Deliberately theme-independent —
// an event's color tag stays the same regardless of light/dark mode, so these
// live outside the mica/midnight palettes in palette.ts.
export const EVENT_COLORS = [
  '#C86B5A',
  '#9F7A45',
  '#547A76',
  '#D6B98C',
  '#6B7FA4',
  '#8A6BA4',
] as const;
